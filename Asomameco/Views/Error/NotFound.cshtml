@{
    ViewData["Title"] = "Página No Encontrada";
}

<div class="container d-flex justify-content-center align-items-center" style="min-height: 100vh;">
    <div class="row shadow-lg p-5 rounded text-center" style="width: 800px; background-color: white;">
        <!-- Columna izquierda: Imagen -->
        <div class="col-md-6 d-flex flex-column align-items-center justify-content-center">
            <img src="~/images/404.svg" alt="Error 404" class="img-fluid" style="max-width: 300px;" />
        </div>

        <!-- Divisor vertical -->
        <div class="col-md-1 d-flex align-items-center justify-content-center">
            <div style="width: 1px; height: 100%; background-color: #e4872c;"></div>
        </div>

        <!-- Columna derecha: Mensaje -->
        <div class="col-md-5">
            <h2 class="mb-3 text-center" style="color: #07305d; font-weight: bold;">Oops... Página No Encontrada</h2>
            <p style="color: #333;">Parece que la página que buscas no existe o ha sido movida.</p>
            <a asp-controller="Home" asp-action="Index" class="btn-return">Volver al inicio</a>
        </div>
    </div>
</div>

<style>
    .btn-return {
        display: inline-block;
        background-color: #07305d;
        color: white;
        padding: 12px 24px;
        border: none;
        border-radius: 30px;
        cursor: pointer;
        font-size: 18px;
        font-weight: bold;
        text-decoration: none;
        transition: background-color 0.3s ease, transform 0.2s ease;
    }

        .btn-return:hover {
            background-color: #0b488c;
            transform: scale(1.05);
        }
</style>
